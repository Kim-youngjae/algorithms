package com.programmers.level3.p81303;

import java.util.*;

public class Solution {
    public String solution(int n, int k, String[] cmd) {
        
        // 배열 선언 
        char[] arr = new char[n];

        // arr 배열을 모두 'O'로 초기화
        Arrays.fill(arr, 'O');

        for (int i = 0; i < cmd.length; i++) {
            
        }


        
        
        String answer = "";

        return answer;
    }
}
